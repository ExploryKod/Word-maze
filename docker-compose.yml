version: "3.8"

services:
    backend:
        build: .
        restart: always
        ports:
            - "4000:5000"
        volumes:
            - .:/python-docker

    frontend:
        build: .
        restart: always
        volumes:
            - .:/python-docker

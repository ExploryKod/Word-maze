version: "3.8"

services:
    backend:
        build:
            context: .
            dockerfile: Dockerfile
        restart: always
        ports:
            - "4000:5000"
        volumes:
            - ./:/python-docker

    frontend:
        build:
            context: ./style_config
            dockerfile: Dockerfile
        restart: always
        volumes:
            - ./:/python-docker